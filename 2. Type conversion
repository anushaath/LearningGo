Conversion happens by putting the variable within the braces of type.

Eg:-

  x int = 4
  c float64 = float64(x)
  d uint = uint(c)
  
  
# Zero Values

  int, float = 0
  bool = false
  string = ""
  
  
## Type Inference

i := 42           // int
f := 3.142        // float64
g := 0.867 + 0.5i // complex128


* Sample Program *

package main

import (
	"fmt"
	"math"
)

func main() {
	var x, y int = 3, 4
	var f float64 = math.Sqrt(float64(x*x + y*y))
	var z uint = uint(f)
	fmt.Println(x, y, z, f)
}
